# Secrets — never commit
.env
*.env
!*.env.example

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
*:Zone.Identifier

# Reference material dropped in from other repos (has its own nested .git)
docs/examples/homebase-infra/

# Node / build output (for the future Astro build)
node_modules/
dist/
.astro/
.cache/

# Data pipeline scratch (Parquet is committed deliberately later; ignore local runs)
*.duckdb
*.duckdb.wal
.venv/
__pycache__/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
