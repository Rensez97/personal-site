# Secrets — never commit
.env
*.env
!*.env.example

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
*:Zone.Identifier

# Local-only working material — photo originals, design exports, drafts,
# reference repos. Grouped under one roof so nothing leaks into a public repo.
# See local/README.md.
local/

# Node / build output (for the future Astro build)
node_modules/
dist/
.astro/
.cache/

# Data pipeline scratch (site/data outputs + history/stars.csv ARE committed)
*.duckdb
*.duckdb.wal
.venv/
__pycache__/
pipeline/dbt/target/
pipeline/dbt/logs/
pipeline/dbt/dbt_packages/
.user.yml
.wrangler/
