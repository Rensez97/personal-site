# Secrets — never commit
.env
*.env
!*.env.example

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node / build output (for the future Astro build)
node_modules/
dist/
.astro/
.cache/

# Data pipeline scratch (Parquet is committed deliberately later; ignore local runs)
*.duckdb
*.duckdb.wal
.venv/
__pycache__/
